<%= form_with(model: bookmark_category, local: true) do |form| %>
  <% if bookmark_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bookmark_category.errors.count, "error") %> prohibited this bookmark_category from being saved:</h2>

      <ul>
        <% bookmark_category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :bookmark_id %>
    <%= form.text_field :bookmark_id %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
